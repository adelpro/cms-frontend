---
description:
globs:
alwaysApply: true
---

After every long process (command that makes non-small changes) do a build and check for build errors and solve them
Don't run npm run dev (basically don't serve the app)